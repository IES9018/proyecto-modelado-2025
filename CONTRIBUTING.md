# 🤝 Cómo Contribuir a EzeBlog

Agradecemos tu interés en el proyecto EzeBlog. Para mantener la consistencia y calidad, sigue estas sencillas guías.

## 1. Convención de Commits

Utilizamos el estándar [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) para que nuestro historial de cambios sea claro y legible.

Los prefijos más comunes son:

* **feat:** Una nueva funcionalidad (ej: `feat: agregar sistema de categorías`).
* **fix:** Una corrección de errores (ej: `fix: corregir error de login`).
* **docs:** Cambios solo a la documentación (ej: `docs: expandir README`).
* **refactor:** Cambios al código o estructura sin cambiar funcionalidad (ej: `refactor: renombrar archivos a kebab-case`).

## 2. Versionado

Utilizamos **Versionado Semántico (SemVer)** para gestionar nuestros lanzamientos:

* **MAJOR (X.y.z):** Cambios incompatibles (rompen la compatibilidad).
* **MINOR (x.Y.z):** Nueva funcionalidad sin romper la compatibilidad (ej: `v1.1.0`).
* **PATCH (x.y.Z):** Correcciones de errores compatibles.